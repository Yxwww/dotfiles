{
  "name": "ports",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --compile --outfile pf",
    "install-cli": "bun run build && mkdir -p ~/.local/bin && ln -sf \"$(pwd)/pf\" ~/.local/bin/pf"
  },
  "dependencies": {
    "@opentui/core": "^0.1.87"
  }
}
